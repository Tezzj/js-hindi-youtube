"use strict"; // treats all js code as newer version

//alert(3+3)  // this is node, not browser. So it wont work here

let name = "Tej"  // string
let age = 22      // number
let isLogged = false // boolean datatype

// range of number => 2^53
//bigint -> used for very large numbers
// null => standalone value(use do define empty(not 0))
// undefined => used when value is not defined
// symbol => unique


// object

//typeof tells the datatype of the value
console.log(typeof undefined); // undefined
console.log(typeof null); //output m object show hoga
